# 201 完整工作台测试与验收记录

记录日期：2026-09-15

## 环境

- 数据库：按 `docs/02-technical/deployment/本地开发环境.md` 注入，直连 201 PostgreSQL。
- 模型：`local` 确定性模型，不依赖外部 API Key。
- 服务：FastAPI/Uvicorn，演示进程开启本地自动验证。
- SQLite：未使用；应用启动会拒绝 SQLite URL。

## 自动检查

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| Python 编译 | `PYTHONPATH=src .venv/bin/python -m compileall -q src scripts` | 通过 |
| 单元测试 | `.venv/bin/pytest -q` | 25 passed；包含多页面入口、真实页面导航，以及日志导出、任务、版本和删除边界回归 |
| 用户脚本语法 | `node --check src/userscript/purslyx-job-capture.user.js` | 通过 |
| 正式工作台脚本语法 | `node --check src/web/assets/workbench.js` | 通过；覆盖多页面正式工作台共用交互 |
| 201 Chrome 多页面 E2E | `PURSLYX_BASE_URL=http://127.0.0.1:8001 node scripts/e2e_web_201.mjs` | 通过；求职 10 页、招聘 6 页、管理 5 页，实际完成两类账号注册、资料草稿确认、岗位期望、求职入池与报告、招聘单人分析与报告 |
| 201 正式冒烟 | `PURSLYX_BASE_URL=http://127.0.0.1:8001 PYTHONPATH=src .venv/bin/python scripts/smoke_201.py` | 通过；使用本项目独立端口校验当前代码，含 multipart 文件、岗位版二次保存、待核实事项、针对性面试问题、面试 `early` 总结和级联删除访问撤销 |
| 201 本地 Worker | `PURSLYX_PORT=8002 PURSLYX_EXECUTION_MODE=worker scripts/start_201_local.sh` + `PURSLYX_BASE_URL=http://127.0.0.1:8002 PYTHONPATH=src .venv/bin/python scripts/smoke_worker_201.py` | 通过；HTTP 初始 queued，独立 Worker 完成后为 succeeded，分析和 PDF 为 available，数据库仍为 201 PostgreSQL；验证后已停止 8002 临时服务 |
| 日志导出可选验收 | `PURSLYX_ADMIN_EMAIL=... PURSLYX_ADMIN_PASSWORD=... PYTHONPATH=src .venv/bin/python scripts/smoke_log_export_201.py` | 脚本只在显式注入管理员凭据或短期 Token 时运行；覆盖 POST 202、幂等、时间筛选、inline/Worker 最终 downloadable、CSV 下载。未注入时只检查 201 健康状态并安全跳过 |

补充边界：资料原件、PDF 成品和日志导出统一走容量检查、原子落盘、私有目录校验和禁止公共缓存；业务记录只保存相对存储键，下载文件名由服务端重建。

正式冒烟最后结果：

```json
{
  "environment": "201",
  "database": {"backend": "postgresql", "driver": "postgresql+psycopg"},
  "analysis_status": "available",
  "verification_items": true,
  "interview_questions": true,
  "task_304": true,
  "rewrite_status": "available",
  "pdf_ready": true,
  "multipart_file_ready": true,
  "browser_draft_deduped": true,
  "apply_status": 303,
  "interview_status_after_answer": "awaiting_answer",
  "interview_early_summary": "early",
  "cross_account_isolation": true,
  "delete_old_entry_blocked": true,
  "cascade_delete_access_revoked": true
}
```

覆盖的边界：中性注册 202、错误凭据稳定错误码、错误响应 `request_id/retryable`、Origin 白名单、CSRF 缺失、资料同键重试、同键不同摘要、文字与 multipart 文件上传、确认版本、分析同键重试只扣一次、任务详情 ETag/304、改写采用、岗位版二次版本保存与幂等、PDF 下载、浏览器草稿去重、去投递重复点击、面试刷新恢复、提前结束 `early` 总结、跨账号 404、删除影响快照与 `If-Match`、删除后旧入口 404、删除简历后分析／改写／面试／岗位版详情 404、导出过期且文件下载 404、登出 204。

## Web 界面检查

- 静态检查通过：viewport 元标签存在；900px 和 680px 断点会把侧栏改为横向导航、表单与报告改为单列；工作台脚本语法通过。
- 文件上传控件在渲染后挂入正式资料表单，支持 PDF/DOC/DOCX；请求失败时保留文字、文件对象和标题。
- 正式报告页展示服务端生成的待核实事项和针对性面试问题；局部刷新不会重复插入跟进区。
- 多页面正式工作台已接通真实 API：求职端资料／期望、匹配池、报告、改写、岗位版、面试、任务、用量、统计和反馈；
  招聘端资料／条件／单人分析历史；授权账号的站点概况、用户、角色、次数和日志筛选／导出。
- Chrome 交互级验收已完成，菜单使用真实 URL，报告以 `analysis_id` 深链接恢复；每个 HTML 自身包含页面职责和模块结构，不是空 `#app` 壳。
- 本记录尚未完成逐页截图视觉评审；明日演示前可人工点检文件上传、岗位版编辑、面试总结和管理员日志导出四个高交互页面。

## PDF 视觉检查

最新冒烟生成的 PDF 使用 A4、1 页、ReportLab 输出；`pdfinfo` 可读，文件以 `%PDF` 开头。使用 Poppler 渲染 PNG 后人工检查：中文字体正常、项目符号为 ASCII `-`、标题／正文／页脚无方框、重叠或截断。

## 尚未宣称通过的项目

- 真实 BOSS／猎聘页面的 DOM 兼容性和登录态握手；
- PostgreSQL 并发锁竞争与 20 次额度耗尽的压力验证；
- Celery/Redis 生产队列替换、SMTP 邮件和真实模型供应商联调；本地 PostgreSQL Worker 已完成最小认领与过期恢复代码，但尚未做压力验收；
- 招聘端已纳入 Chrome E2E；管理员五个页面入口和权限保护已自动检查，但管理员写操作仍需显式注入凭据；生产迁移工具未交付。
- 日志导出自动脚本需要管理员权限；本轮未读取或写入真实管理员密码，因此未在本机执行管理员 E2E。明日若不注入凭据，直接展示脚本和单元证据即可。

这些项目属于后续验收，不影响本次“需求—接口—代码—201 实测”的演示闭环。
