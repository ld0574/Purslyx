# 201 最小演示变更记录

| 日期 | 变更 | 原因／验证 |
| --- | --- | --- |
| 2026-09-15 | 固定运行基线为 `本地开发环境.md` 的 201 PostgreSQL；显式拒绝 SQLite 和其他数据库 URL | 避免演示环境漂移；`test_config.py` 通过 |
| 2026-09-15 | 增加正式 API 的认证、CSRF、版本、分析、用量、改写、岗位版、PDF、面试和浏览器草稿链路 | 把需求、接口和代码串成可运行纵向切片 |
| 2026-09-15 | 201 现有库采用不清库的兼容升级；新增当前演示表和早期 `documents`／`apply_click_events` 列兼容 | 保留已有演示数据；启动和正式冒烟通过 |
| 2026-09-15 | 修复文件上传 `StoredFile` 原始文件名引用和 MIME；收紧版本归属、简历／JD 类型校验；补回分析任务与报告关联 | 修复 multipart 运行时错误和幂等重试缺陷；编译、冒烟通过 |
| 2026-09-15 | 正式 `smoke_201.py` 改为注册—登录—草稿—版本—报告—改写—PDF—浏览器—面试全链路 | 现场可用一条命令复核，不依赖页面状态 |
| 2026-09-15 | 删除改为先读取影响快照并携带 `If-Match`，成功返回 204；任务详情补充 ETag/304 和原任务重试 | 防止关联变化时误删；正式冒烟已覆盖删除确认和任务 304 |
| 2026-09-15 | 正式工作台补齐任务、事实／逐段改写、去投递、影响确认删除及招聘／管理员入口 | 页面动作与已有 API 对齐；JavaScript 语法检查通过，管理员端到端仍列为后续验收 |
| 2026-09-15 | 浏览器脚本增加本机待上传草稿续传和 token 更新事件；页面增加 201 PostgreSQL 健康徽标 | 覆盖未登录、平台失败和现场连接确认 |
| 2026-09-15 | 统一资料上传、PDF 和日志导出的私有存储边界：容量检查、原子写入、相对存储键、目录穿越校验、安全下载文件名和到期阻断 | 保持 201 演示文件可下载，同时不把主机绝对路径暴露到业务记录；正式冒烟继续通过 |
| 2026-09-15 | 增加 Feature Spec、追溯矩阵、实施计划、演示脚本和测试记录 | 明天可直接讲 SDD 过程和诚实边界 |
| 2026-09-15 | 正式冒烟增加资料级联删除验收：分析、改写、面试、岗位版详情立即 404，导出文件立即不可下载 | 用同一条 HTTP 链路验证删除影响快照、`If-Match`、派生正文撤销和物理 PDF 清理 |
| 2026-09-15 | 修复岗位版保存把内部 `job_pool_item_id` 当成外部 `public_id` 查询；幂等命中提前返回；面试总结统一为 `full`／`early` | PostgreSQL 岗位版二次保存和提前结束冒烟通过；新增 3 个 SDD 契约回归断言 |
| 2026-09-15 | multipart 元字段使用 Pydantic 校验；工作台动态插入 PDF/DOC/DOCX 上传控件并在失败后恢复文件选择 | 收紧上传契约，修正控件未挂入 DOM 的静态复核问题；前端脚本检查通过 |
| 2026-09-15 | 报告新增待核实事项与针对性面试问题；正式报告页和最短演示页统一展示；PDF 导出统一接入本地任务执行模式 | 正式 201 冒烟断言两组报告字段；内联与 PostgreSQL Worker 模式均通过，Worker 初始 queued、最终分析与 PDF available |

## 当前提交边界

本记录不把真实模型效果、真实平台投递结果、Worker 恢复或并发压测写成已完成。运行时 `data/`、`tmp/` 和带凭据的 `本地开发环境.md` 均不提交 Git。
