# Purslyx 首版模块与页面清单

| 项 | 内容 |
| --- | --- |
| 版本 | v0.6 |
| 更新日期 | 2026-09-14 |
| 状态 | 待小组评审；供产品、原型和开发共用，不代表功能已实现 |
| 交付窗口 | 七天黑客松首版，基础与增强能力均须验收 |
| 依据 | [需求说明](需求说明.md)、[技术架构](../02-technical/技术架构.md)、[A 视觉方案](视觉方案.md) |

这份清单只回答三个问题：有哪些模块、两类用户怎样完成任务、工作台菜单打开哪些页面。注册时须选择“求职”或“招聘”；首版身份固定，没有工作区切换，另一身份需要用另一邮箱注册。求职者有自己的私有匹配池；招聘者只分析一个岗位与一位候选人，不建立候选人池。

首版必须完成 BOSS 直聘与猎聘当前岗位获取、逐条匹配打分、完整文字面试，以及从匹配池跳转到原岗位详情的“去投递”入口与点击统计。管理端的用户、后台权限、次数发放和站点概况也属于首版。用户进入原平台后自行决定是否投递；Purslyx 不代填、不自动提交，也不监控平台投递进度或回复。公开岗位市场、自动发现岗位、跨岗位推荐、真实支付和企业团队功能不在首版范围。现有[视觉预览](视觉预览.html)只作为视觉方向，菜单和页面范围以本清单为准。

## 一、模块与功能

模块名固定为下表的短名，后续文档也按这些名字命名。“简历”负责资料、条件、分析、改写和 PDF；“匹配池”负责求职者保存的岗位及其匹配进度，浏览器脚本是匹配池的输入入口。管理端复用“用户”“用量”“统计”三个模块：分别管理账号与后台权限、发放使用次数、查看全站概况。

| 模块 | 负责什么 | 首版功能列表 |
| --- | --- | --- |
| 用户 | 管理固定身份的账号和私有数据 | 邮箱密码注册时选择求职或招聘、邮箱验证与找回密码、登录退出；首版不切换身份；重新登录后恢复本账号资料与历史；按账号隔离访问；删除前预览影响。授权管理员可搜索和查看用户，管理独立的后台角色与权限并留操作记录；注册身份固定，不能通过后台改成另一身份。 |
| 简历 | 完成一份简历与一个岗位的准备、分析和成品 | 求职者导入自己的简历，招聘者导入候选人简历；支持文本、文字型 PDF、DOC、DOCX；检查并确认岗位描述；保存求职期望或候选人明确提供的期望及来源；展示能力分、原文依据、岗位条件对照和整体建议；求职者可补充事实、改写、编辑岗位版并导出同版本 PDF；招聘者得到单人报告与待核实问题。 |
| 匹配池 | 收集求职者确认的岗位，并逐条展示匹配结果 | 篡改猴在 BOSS 直聘、猎聘当前岗位页由用户主动获取信息，或由用户手动粘贴；检查后保存为私有岗位；用户在入池时确认一次分析消耗，资料齐全且剩余次数允许便自动对这一岗位打分；不满足条件时保留“待分析”；岗位详情提供“去投递”，点击后记录点击并发起打开原 JD 页面。 |
| 面试 | 围绕一个已确认岗位完成文字练习 | 生成三道主问题，每题最多一次承接回答的追问；逐轮反馈、提前结束、完成汇总和中断续答；未回答部分明确标记，不重复扣除使用次数。 |
| 任务 | 让耗时操作可查看、重试和恢复 | 导入、分析、改写、导出与面试显示排队、执行、待补充、失败和完成；重复点击回到原任务，断线或重新登录后可继续；失败保留输入。 |
| 用量 | 管理各功能的剩余次数和发放记录 | 求职账号默认有分析 20 次、改写 20 次、面试 5 场，招聘账号默认有分析 20 次；执行前展示消耗与剩余次数，成功只扣一次，失败释放预留；免费操作不扣次数；频率、并发和每日预算受限时提示并保留输入。授权管理员可按适用功能给指定用户追加次数，填写原因并查看发放与使用记录。 |
| 统计 | 查看使用、去投递跳转与产品反馈 | 求职者查看本人今日“去投递”点击数，招聘者查看自己的候选人分析次数；两类用户可提交对 Purslyx 的使用反馈。授权人员查看全站概况、注册用户、按日期／身份／功能汇总的使用、成本、去投递点击和产品反馈；点击数不当作平台实际投递成功数。 |

统计口径写作“去投递点击数”：服务端校验并记录一次用户点击，随后返回原岗位详情的跳转；无法确认浏览器是否收到跳转响应或外部网页最终加载。刷新、同一点击的重复请求、保存岗位或导出 PDF 不新增计数。这个数字表示 Purslyx 带来的投递意向，**不能写作“已投递简历数”或平台成功投递数**。平台的实际提交、回复和面试进度由用户在原平台查看，首版不自动读取。产品反馈指用户主动评价 Purslyx，与去投递点击数分别统计。

## 二、主流程怎么跑

注册、验证和登录后，用户直接进入注册身份对应的工作台。工作台显示近期内容和未完成任务，具体操作在菜单页面完成；不会出现切换到另一身份的按钮。获授权人员另有管理端入口，注册身份仍固定。所有计次 AI 操作都先展示消耗和剩余次数，任务进行中可离开页面，之后从工作台或历史恢复。

**求职流程**

1. 从“简历”导入并确认自己的简历，设置默认与本次求职期望：岗位方向、地点／工作方式、薪资和条件强度。未填写保持“未知”，不能当成“不限”。
2. 在 BOSS 直聘或猎聘的当前岗位页主动点击“获取岗位”，或在“匹配池”手动粘贴岗位描述；回到 Purslyx 检查岗位名称、职责、地点／方式和薪资，确认后保存到自己的匹配池。
3. 入池确认页同时说明这条岗位自动分析会消耗一次分析次数。简历、期望和剩余次数都满足时，保存后自动为该岗位排队打分；缺资料、可用次数或全站预算时先保存为“待分析”，补齐后再确认消耗并分析。每条岗位分别有报告，不自动比较或推荐多个岗位。
4. 打开匹配池里的岗位，阅读能力依据、条件对照和整体建议。需要优化表达时进入“简历”，补充真实事实、确认消耗 1 次改写机会、比较和采用建议，再编辑岗位版并下载 PDF；也可从“面试”开始或恢复文字练习。
5. 从匹配池岗位详情点击“去投递”，Purslyx 记录一次点击并发起跳转到该岗位在 BOSS 直聘或猎聘的原详情页。用户在平台页面自行投递和查看回复；返回 Purslyx 不需要等待脚本识别投递状态。
6. 从“统计”查看今日去投递点击数并提交产品使用反馈，从“用量”查看剩余次数；资料、岗位和历史可重新打开，删除前先确认关联影响。

**招聘流程**

1. 注册时选择招聘身份，从“候选人简历”导入一份岗位描述和一位候选人的简历，检查并确认提取内容与岗位条件。
2. 候选人明确提供期望时才填写岗位方向、地点／方式与薪资，并记录来源；未提供的项目标为“未知”，不替候选人推断。
3. 确认消耗一次分析次数，查看能力分及原文依据、条件冲突、待核实事项和面试问题。报告供招聘者人工判断，不自动淘汰或录用，也不进入求职者的匹配池、改写和面试练习。
4. 从“候选人简历”重新打开原报告及所用资料版本；条件变化时明确确认后另开一次分析。从“统计”查看自己的分析次数，从“用量”查看剩余次数。

实现上先打通用户、任务和用量，再完成简历与招聘单人分析，随后完成匹配池、两站浏览器输入和去投递跳转、面试，最后联验管理端和统计。顺序只表示依赖，上述首版功能都要完成。桌面端覆盖全流程；手机至少能登录、上传或粘贴资料、确认条件、发起并阅读分析、回答面试、阅读和下载 PDF，以及查看匹配池与原岗位链接。复杂简历排版和浏览器脚本以桌面为主。

## 三、模块页面与工作台菜单

**求职工作台菜单**：工作台 → 简历 → 匹配池 → 面试 → 用量 → 统计。

**招聘工作台菜单**：工作台 → 候选人简历 → 用量 → 统计。

**管理端菜单**：站点概况 → 用户管理 → 次数管理。入口仅向有相应后台权限的账号显示；每页再次校验操作权限。“用户管理”“次数管理”“站点概况”分别属于用户、用量、统计模块。

“候选人简历”和“简历”由同一简历模块按账号身份展示不同功能。右上角账号菜单提供本账号信息、退出及授权人员可见的“进入管理端”，没有身份切换。“任务”显示在发起页，工作台有继续入口，不占单独菜单；浏览器侧“获取岗位”入口在 BOSS 直聘和猎聘当前岗位页。

下表按模块列出页面与界面，菜单名就是原型需要呈现的入口。各私有页面都要校验账号归属；已删除内容从旧入口打开时显示不可访问。空内容、处理中、失败和完成状态都需用文字说明，失败时保留可恢复的输入。

| 模块 | 对应菜单或入口 | 页面／界面、进入与返回 | 主要操作与状态 | 手机要求 |
| --- | --- | --- | --- | --- |
| 用户 | 公开入口 | 注册与登录：公开进入；注册时选固定身份，未验证去邮箱验证，已验证登录后去对应工作台 | 展示身份选择、提交中、凭据或限频错误、进入工作台；选定身份首版不可改 | 必须可操作 |
| 用户 | 邮件链接、登录页 | 邮箱验证与找回密码：从邮件或登录页进入，完成后回登录页或工作台 | 验证、重发和重置；展示待验证、发送中、链接失效、成功 | 必须可操作 |
| 用户 | 工作台、账号菜单 | 工作台首页：登录后进入；可去本身份的各菜单并返回 | 查看最近资料、匹配或分析、未完成任务；展示无内容引导、恢复中、加载失败、可打开结果；账号菜单可退出 | 必须可导航 |
| 用户 | 管理端：用户管理 | 用户列表与详情：从管理端菜单进入，返回管理端 | 按邮箱、注册身份和状态搜索账号，查看注册及使用概况；展示无结果、加载中、无权限或失败、详情完成；不得浏览私有简历正文或修改注册身份 | 桌面主用；手机可读 |
| 用户 | 管理端：用户管理 | 后台角色与权限：从用户详情或角色入口进入，保存后回原入口 | 超级管理员维护角色的权限组合，为账号授予或撤销后台角色；展示无授权、提交中、冲突或越权、更新成功；保留操作者及前后值，超级管理员内置角色不可编辑，管理者不能改自己的角色，也不能移除最后一个超级管理员 | 桌面主用；手机可读 |
| 简历 | 简历／候选人简历 | 资料列表：从菜单进入；返回工作台或进入导入与确认 | 求职者看自己的简历，招聘者看候选人简历及岗位描述；展示无资料、加载中、读取失败、有效列表；删除先预览影响 | 必须可查看和导入 |
| 简历 | 简历／候选人简历 | 导入与确认：从资料列表进入；返回列表，确认后进入本次分析或匹配池 | 上传或粘贴、纠正提取内容和岗位条件，保存确认版本；展示无输入、解析中、失败保留输入、确认完成 | 必须可上传、粘贴、纠正 |
| 简历 | 简历 | 求职期望：从简历菜单或分析前确认进入；保存后回原入口 | 设置默认与本次岗位方向、地点／方式、薪资及强度；区分未知、不限、面议；展示未填写、保存中、字段错误、已保存 | 必须可操作 |
| 简历 | 候选人简历 | 候选人条件确认区：随招聘导入页打开；完成后进入单人分析 | 只记录候选人明确提供的期望及来源；展示未知、确认中、字段错误、已确认版本 | 必须可操作 |
| 简历 | 简历／候选人简历、匹配池 | 分析历史与详情：从菜单、导入结果或匹配池岗位进入；返回原入口 | 同一量表展示能力分、依据、条件对照和版本；招聘端增加待核实问题；展示无报告、执行中、失败可重试、完整报告 | 必须可阅读 |
| 简历 | 简历、分析详情 | 事实补充与改写对照：求职者从报告或简历菜单进入，返回原入口或进入编辑 | 保存补充事实，确认消耗 1 次改写机会，对照原文与建议、编辑和采用；展示无建议、生成中、校验失败、已保存 | 必须可阅读；复杂编辑可转桌面 |
| 简历 | 简历 | 岗位版简历列表与编辑：从菜单或改写对照进入，返回列表或报告 | 查看已保存岗位版、调整顺序与版式、预览并下载同版本 PDF；展示无内容、导出中、溢出或导出失败、可读成品 | 必须可阅读和下载 PDF；复杂排版用桌面 |
| 匹配池 | 匹配池、浏览器侧 | 获取岗位与入池确认：从 BOSS／猎聘当前页或手动粘贴进入，确认后回匹配池 | 检查来源和缺失字段，展示一次分析消耗；展示无岗位、获取中、页面或登录失败、已入池待分析／分析中 | Web 手机可手动粘贴；脚本桌面必交 |
| 匹配池 | 匹配池 | 私有岗位列表与详情：从菜单进入；打开条目后可去报告、简历或原平台，返回列表 | 看逐条分数、依据、输入版本和待分析原因；展示空池、分析中、失败、可读结果；“去投递”只在有有效原岗位链接时可用 | 必须可查看与继续分析 |
| 匹配池 | 岗位详情 | 去投递跳转：从已保存的岗位详情点击，记录后发起前往原 JD 页面；用户可返回匹配池 | 展示可打开链接、记录中、链接失效或记录失败、已发起跳转；同一次点击不重复计数，不声称平台已投递 | 手机和桌面均须可跳转 |
| 面试 | 面试、分析详情 | 面试列表与开场：从菜单或报告进入，返回原入口或进入会话 | 选定岗位与简历，确认消耗 1 场面试机会，开始或恢复；展示无会话、生成中、失败保留输入、题目完成 | 必须可开始和恢复 |
| 面试 | 面试 | 面试会话与汇总：从列表进入，结束后回列表或报告 | 逐题回答、查看反馈、最多一次追问、提前结束或完成汇总；展示未答、生成中、中断可恢复、汇总完成 | 必须可作答和阅读 |
| 任务 | 工作台、各操作发起页 | 任务状态与继续入口：附着于导入、分析、改写、导出和面试页，从工作台返回原任务 | 展示排队、执行、待补充、失败和完成；重试保留输入，重复点击回到原任务 | 关键任务必须可查看和恢复 |
| 用量 | 用量、计次操作确认层 | 剩余次数与使用记录：从菜单或分析／改写／面试入口进入，确认后回原操作 | 只显示本身份可使用的功能及剩余次数，执行前确认消耗；展示无使用记录、处理中、剩余次数或预算不足、结算或释放结果 | 必须可查看和确认 |
| 用量 | 管理端：次数管理 | 次数发放与记录：从管理端菜单进入，选用户后返回用户详情或次数管理 | 选择该身份适用的功能、追加次数并填写原因；查看发放和消耗流水；展示无记录、提交中、越权或冲突、发放成功；重复提交不重复发放 | 桌面主用；手机可读 |
| 统计 | 统计 | 我的统计：从菜单进入，返回工作台或匹配池 | 求职者看今日去投递点击数，招聘者看自己的分析次数；产品使用反馈另开区域；展示无记录、加载中、读取失败、可追溯统计 | 必须可查看 |
| 统计 | 管理端：站点概况 | 站点概况：从管理端菜单进入，返回管理端或本人的工作台 | 查看注册用户及身份分布、分析／改写／面试、今日去投递点击数、成本与产品反馈；按日期筛选，未知成本单列；展示无数据、加载中、无权限或失败、概况完成 | 桌面主用；手机可读 |

资料、岗位、报告、岗位版简历和面试历史删除都先展示关联影响；确认后旧入口立即不可读，文件与正文随后清理。去投递跳转记录与产品反馈分开统计，也不推断平台投递结果。
